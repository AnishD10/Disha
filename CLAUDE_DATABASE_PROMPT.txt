You are analyzing the Disha project database for a university assignment.

Use the repository files as the source of truth:
- database/schema.sql
- database/init.sql
- database/setup.sql
- DIAGRAMS/06-Entity-Relationship-Diagram.mmd
- DIAGRAMS/05-Complete-Architecture-Layered.mmd
- DATABASE_AND_DAO_CONVENTIONS.md

Task
Create a complete database design and development write-up based on the pasted assignment requirements below.

Assignment requirements to cover
1. Database design and development.
2. Entity Relationship Diagram design with proper business rules.
3. Validation that all tables are normalized up to 3NF.
4. Table-by-table database development documentation with sample data.
5. Screenshot guidance for each table and for the overall relationship diagram.

What to produce
1. An ERD/business-rules explanation.
2. A 3NF validation section.
3. A data dictionary for every table in the schema.
4. Sample data notes for each table.
5. A screenshot checklist for database-tool evidence.

Schema areas you must cover
- users and user_sessions
- student_profiles, parent_student_links, student_constraints
- assessments, questions, question_options, student_assessments, student_answers, aptitude_profiles
- careers, career_aptitude_mapping, career_matches
- colleges, degrees, decision_plans
- counselor_assignments, counselor_reports
- labour_market_data, admin_logs, system_feedback

Instructions for the ERD section
- Describe the entities and relationships using the foreign keys in schema.sql.
- State the business rule for each relationship.
- Mention cardinality where relevant, such as one-to-one, one-to-many, and many-to-many.
- Refer to the ERD file in DIAGRAMS/06-Entity-Relationship-Diagram.mmd as the diagram source.

Instructions for the 3NF section
- Explain normalization table by table or by logical group.
- For each table, state the primary key, important attributes, and why the table is in 1NF, 2NF, and 3NF.
- Mention any intentional reporting or derived tables separately.
- Do not claim perfect normalization without review.
- Flag student_constraints as a possible normalization concern if preferred_locations or preferred_career_paths are stored as multi-value text fields.

Instructions for the data dictionary
- Create a row for every table.
- Include table name, purpose, primary key, foreign keys, key columns, and notes.
- Include sample data guidance for each table.
- Use database/init.sql as the base for starter rows.
- If a table has no sample data in init.sql, say that new sample rows must be created before screenshots can be taken.

Instructions for the screenshot checklist
- List every table that should be captured in the database tool.
- Tell the user to show the table structure and valid data in each screenshot.
- Tell the user to capture the overall relationship diagram screenshot from the tool they use, such as XAMPP/phpMyAdmin or MySQL Workbench.
- Mention that screenshots should be readable and show the table name, columns, and sample rows clearly.

Useful project facts
- The schema is already defined in database/schema.sql.
- Initial seed data exists in database/init.sql, mainly for admin, assessments, questions, and question options.
- The database setup script is in database/setup.sql.
- The project already includes an ERD file and a layered architecture diagram set.
- The DAO and database naming rules are documented in DATABASE_AND_DAO_CONVENTIONS.md.

Output style
- Write in a clean academic style.
- Make the content suitable for pasting into a report or assignment document.
- Be specific to the Disha project, not generic database theory.
- Where normalization is discussed, ground the explanation in the actual schema.

Finish with a short conclusion explaining how the database supports the Disha career intelligence portal.