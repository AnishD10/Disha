<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
		 https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd" version="6.0">

    <display-name>DISHA - Nepal Career Intelligence Portal</display-name>

    <!-- ── Session Configuration ──────────────────────────────────── -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <!-- Set secure=true when deployed over HTTPS in production -->
            <!-- <secure>true</secure> -->
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- ── Welcome Files ──────────────────────────────────────────── -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- ── Error Pages ────────────────────────────────────────────── -->
    <error-page>
        <error-code>403</error-code>
        <location>/pages/error/403.jsp</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/pages/error/404.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/pages/error/500.jsp</location>
    </error-page>

    <!--
        NOTE: Servlet and Filter mappings are handled via @WebServlet and @WebFilter
        annotations in the respective Java classes:
          - UserServlet    → /auth/register, /auth/login, /auth/logout
          - DecisionServlet → /decision/plan
          - SessionFilter  → /* (all paths)
    -->

</web-app>
